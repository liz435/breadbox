# Domain Docs

This repository uses one shared domain context. Engineering skills should read `CONTEXT.md` and relevant files in `docs/adr/` before working in an unfamiliar area. If those files do not exist, proceed without them.

Use the terminology defined in `CONTEXT.md` when it exists, and surface any conflict with a relevant ADR rather than silently overriding it.
