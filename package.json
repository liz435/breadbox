{
  "name": "dreamer",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run dev:api & bun run dev:app",
    "dev:app": "bun run --cwd packages/app dev",
    "dev:api": "bun run --cwd packages/api dev",
    "build": "bun run --cwd packages/app build",
    "typecheck": "bun run --cwd packages/config typecheck && bun run --cwd packages/schemas typecheck && bun run --cwd packages/app typecheck && bun run --cwd packages/api typecheck"
  }
}
