// Plain-LED lighting pilot. Keep this isolated so we can quickly compare,
// tune, or revert without disturbing the rest of the board rendering stack.
export const REALISTIC_LED_LIGHTING_PILOT = true;
